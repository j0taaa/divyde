# Database connection string used by the web app and local development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/divyde

# Secret used by BetterAuth session handling (set to a strong, unique value in production)
BETTERAUTH_SECRET=changeme
